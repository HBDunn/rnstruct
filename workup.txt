#Migrate from React-native 0.21
>need a way-back machine..
> easier to start over with ```npm i react-native``` 
## Fixes

* ```javascript
	Import React from 'react'
  ```
###Events

Replaced EventEmitter with [EventEmitter3]https://stackoverflow.com/questions/36774540/eventemitter-and-subscriber-es6-syntax-with-react-native
```javascript
	EventEmitter
``` 
>  ```javascript
	  this.on
   ``` 
   in prototype as this is not yet constructed no reference to this.prototype
* ```javascript
	  import { EventEmitter } from 'EventEmitter3';
  ```

#uniqid
https://www.npmjs.com/package/react-native-uuid
import uuid from 'react-native-uuid';
in module replace 'uniqid' with uuid;
#keymirror
Hi-Jacked keyMirror into /utils
https://github.com/STRML/keyMirror/blob/master/index.js
#Buffer
You need to add "buffer" to your package.json dependencies. There's some issues w/ recursive dependencies 
not being resolved with NPM so you need to resolve them yourself.

#cmd-line emulator 
https://stackoverflow.com/questions/4974568/how-do-i-launch-the-android-emulator-from-the-command-line
#adb log
$ adb logcat *:S ReactNativeJS:V ReactNative:V^C
clear
$ adb logcat -c 
#checking on server state - requires elevation
	cmd 
		netstat -bon | find "node.exe"
		taskkill /f /im "node.exe" then check no 8081 netstat -bon | find "8081"
		watch adb.exe too
		tasklist - with | find
	bash 
		netstat -bon | grep -C3 "node.exe"
		tasklist - with a | grep 
#avd 
startemu28.sh
`#!/usr/bin/bash 
echo 'available avds'
/c/Users/PEllc/AppData/local/Android/sdk/emulator/emulator -list-avds
echo 'starting Nexus_5X_API_28_x86 - edit startemu28.sh to change'
/c/Users/PEllc/AppData/local/Android/sdk/emulator/emulator -avd Nexus_5X_API_28_x86
#Nexus_5X_API_27
#Nexus_5X_API_28_x86
`
#tests
#remove Phantom.shim.js
#mock uuid
https://github.com/facebook/jest/issues/2172
::code

` jest.mock("uuid/v4", () => {
	let value = 0;
	return () => value++;
  });`

 ::fix imports remove requires
 ::set uuid sequencially from 0
 :: match register to acctual and reset each time in beforeEach()
 https://stackoverflow.com/questions/39321408/testing-react-flux-store-with-jest-in-es6
 
#rename to action CalculatorActions 
#snapshot and getBaseChildren for granular look at App View tests
https://jestjs.io/docs/en/tutorial-react-native.html
#rename files to .spec.js

