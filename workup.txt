#migrate
#React from 'react'
#events
https://stackoverflow.com/questions/36774540/eventemitter-and-subscriber-es6-syntax-with-react-native
import { EventEmitter } from 'EventEmitter';
import { EventEmitter } from 'EventEmitter3';
const EE = new EventEmitter();
change this.on in prototype as this is not yet constucted no reference to this.prototype

#uniqid
https://www.npmjs.com/package/react-native-uuid
import uuid from 'react-native-uuid';
in module replace 'uniqid' with uuid;
#keymirror
Hi-Jacked keyMirror into /utils
https://github.com/STRML/keyMirror/blob/master/index.js
#Buffer
You need to add "buffer" to your package.json dependencies. There's some issues w/ recursive dependencies 
not being resolved with NPM so you need to resolve them yourself.

#cmd-line emulator 
https://stackoverflow.com/questions/4974568/how-do-i-launch-the-android-emulator-from-the-command-line
#adb log
$ adb logcat *:S ReactNativeJS:V ReactNative:V^C
clear
$ adb logcat -c 
#checking on server state - requires elevation
	cmd 
		netstat -bon | find "node.exe"
		taskkill /f /im "node.exe" then check no 8081 netstat -bon | find "8081"
		watch adb.exe too
		tasklist - with | find
	bash 
		netstat -bon | grep -C3 "node.exe"
		tasklist - with a | grep 
#avd 
startemu28.sh
`#!/usr/bin/bash 
echo 'available avds'
/c/Users/PEllc/AppData/local/Android/sdk/emulator/emulator -list-avds
echo 'starting Nexus_5X_API_28_x86 - edit startemu28.sh to change'
/c/Users/PEllc/AppData/local/Android/sdk/emulator/emulator -avd Nexus_5X_API_28_x86
#Nexus_5X_API_27
#Nexus_5X_API_28_x86
`
